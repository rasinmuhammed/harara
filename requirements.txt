# core
pandas>=2.0
numpy>=1.24
scipy>=1.11
pydantic>=2.0
requests>=2.31

# WBGT / physiology / ML
thermofeel>=2.3
scikit-learn>=1.3
lightgbm>=4.0            # needs the OpenMP runtime: `brew install libomp` on macOS

# data acquisition (ERA5 path is optional; see docs/technical_report.md S2)
cdsapi>=0.7
xarray>=2024.1
netcdf4>=1.6

# figures & tests
matplotlib>=3.8
pyarrow>=14.0
pytest>=8.0
