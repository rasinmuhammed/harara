# Runtime for the Harara API. Self-contained: it deliberately does NOT pull the
# repo's root requirements.txt, which installs ML / data-science packages
# (lightgbm, scikit-learn, xarray, cdsapi, matplotlib, ...) that the API never
# imports. Install for the service:
#     pip install -r api/requirements.txt
fastapi>=0.111
uvicorn[standard]>=0.30
numpy>=1.24
scipy>=1.11
pandas>=2.0
pydantic>=2.0
requests>=2.31
thermofeel>=2.3
httpx>=0.27          # FastAPI TestClient — used by the tests only
