# HTTP service layer, on top of the repo's requirements.txt.
#   pip install -r requirements.txt -r api/requirements.txt
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27          # FastAPI TestClient
