# Base URL of the Harara API (api/). The Next route handlers proxy to it
# server-side; nothing here is exposed to the browser.
API_BASE=http://127.0.0.1:8000
# Vercel: set API_BASE to the deployed API origin, e.g. https://harara-api.onrender.com
